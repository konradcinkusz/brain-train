% ============================================================
%  Trening Mózgu -- A4, 11pt
%  konradcinkusz
%
%  Thin on purpose: the format lives in preamble.tex and the contents in
%  structure.tex, so adding a set or a paper size never means editing the
%  book's wiring twice.
%
%  Build:  make book        -- from the repository root, gated on the log
%
%  Do NOT trust latexmk's exit code. Under -interaction=nonstopmode a failed
%  run still writes a PDF, and with -file-line-error an error line begins with
%  a path rather than "!", so `grep '^!'` sees nothing either.
%  tools/checklog.py is the gate.
% ============================================================

\documentclass[11pt,twoside,openright]{book}

% What this volume is called, and where its generated plan lives. Everything
% else in book/frontmatter/ is shared with the advanced volume unchanged.
\newcommand{\btvoltitle}{Trening Mózgu}
\newcommand{\btvolsub}{Zbiór zadań na czas — bez kalkulatora}
\newcommand{\btplandir}{plan/generated}

\input{preamble}

\hypersetup{
  pdftitle={Trening Mózgu},
  pdfauthor={konradcinkusz},
}

\begin{document}

\frontmatter
\input{frontmatter/title}
\tableofcontents
\input{frontmatter/how-to-use}
\input{frontmatter/plan}

\mainmatter
\input{structure}

\backmatter
\odpowiedzi

\end{document}
